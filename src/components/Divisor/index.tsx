import { View } from "react-native";

export const Divisor = () => (
  <View
    style={{
      width: "auto",
      borderBottomColor: "#CCC",
      borderBottomWidth: 1,
      borderStyle: "solid",
    }}
  />
);
